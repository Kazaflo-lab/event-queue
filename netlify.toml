[build]
  command = "npm run build"
  publish = "dist"

# This section tells Netlify's build system to ignore 
# secret scanning errors that are blocking the deployment.
[build.processing]
  skip_processing = true

[secrets]
  SECRETS_SCAN_SMART_DETECTION_ENABLED = false
  SECRETS_SCAN_ENABLED = false
